title: Use case title
output: use-case-id
steps:
  - action: comment
    narration: >
      Introduce the use case in plain language. Mention prerequisites only
      when the operator must know them for this command.

  - action: type
    command: bash hitt.sh -m 'info cluster'
    narration: Describe what the command does while it runs on screen.
    pause: 0.5

  - action: run
    pause: 0.5
