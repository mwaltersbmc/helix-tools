title: Use case title
output: use-case-id
steps:
  - action: comment
    narration: >
      Introduce the use case in plain language (max ~25 words per step for
      readable captions).

  - action: type
    command: bash hitt.sh -m 'info cluster'
    narration: Short line synced with the command on screen.
    pause: 0.5

  - action: run
    pause: 0.5
